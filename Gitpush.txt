
New branch for every feature/issue

git checkout -b branch_name

git add .

git commit branch_name -m "Nachricht"

//zumindest beim ersten mal nicht ganz sicher ob danach auch

git remote add origin https://stea.iue.tuwien.ac.at/CppProjects2023/GroupNProject.git
git remote rename origin upstream
git push -u origin branch_name

git push  



#How to get branches from the server (I think only needed once)
git branch -a
git checkout branch_name

#How to update branches from the server
git pull    #Only updates the current branch
git fetch   #Updates every branch




